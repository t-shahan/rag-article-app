# FastAPI — web framework
fastapi
uvicorn[standard]
slowapi

# Auth — JWT tokens + password hashing
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1

# OpenAI — LLM + embeddings (used by rag_chain.py)
openai

# MongoDB — vector store + conversation history
pymongo

# AWS S3 — article storage (used by rag_chain.py)
boto3

# Environment variable management
python-dotenv
